# Non-secret defaults baked into the deployed function at deploy time.
# Override locally (this file is tracked) as needed for other projects.
REMINDERS_ENABLED=true
APP_ORIGIN=https://warranty-tracker-33dc5.web.app
